name: Seal demo

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: pip install cryptography rfc8785
      - run: python -m unittest tests/test_ceyo.py -v
      - run: python seal_artifact.py
      - run: python tools/ceyo_verify.py example_artifact/sealed_artifact.json example_artifact/public_key.pem
      - uses: actions/upload-artifact@v4
        with:
          name: sealed-output
          path: |
            example_artifact/sealed_artifact.json
            example_artifact/public_key.pem
